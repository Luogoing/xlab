export function compareEvidence(records,analysis){return records.map(r=>({record_id:r.record_id,publication_number:r.publication_number,title:r.title,applicants:r.applicants,coverage:{abstract:r.evidence.some(e=>e.field==='abstract_excerpt'),claims:r.evidence.some(e=>e.field==='claims'),description:r.evidence.some(e=>e.field==='description')},routes:analysis.groups.filter(g=>g.record_ids.includes(r.record_id)).map(g=>({label:g.label,evidence_ids:g.evidence_ids.filter(id=>r.evidence.some(e=>e.evidence_id===id))})),evidence_count:r.evidence.length}));}
export function findEvidence(record,query=''){const term=query.trim().toLocaleLowerCase();return record.evidence.filter(e=>!term||[e.evidence_id,e.text,e.locator,e.field].some(x=>String(x||'').toLocaleLowerCase().includes(term)));}
