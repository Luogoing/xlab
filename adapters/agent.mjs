import {buildStrategy,normalizeRecords,createTask,reportMarkdown,AppError} from '../core/engine.mjs';
export const TOOL_DEFINITIONS=[{name:'build_patent_strategy',description:'生成可修改的中英文词项策略；规则处理，不查询外部数据库。',inputSchema:{type:'object',properties:{topic:{type:'string',minLength:1,maxLength:500}},required:['topic'],additionalProperties:false}},{name:'explore_patent_records',description:'对明确提供的记录使用共用核心筛选，返回任务、证据和报告；默认用户导入，不做实时全库检索。',inputSchema:{type:'object',properties:{topic:{type:'string',minLength:1,maxLength:500},records:{type:'array',maxItems:2000}},required:['topic','records'],additionalProperties:false}}];
export function executeTool(name,args){if(!args||typeof args.topic!=='string')throw new AppError('INVALID_ARGUMENTS','topic 必须为非空字符串');if(name==='build_patent_strategy')return buildStrategy(args.topic);if(name==='explore_patent_records'){const d=normalizeRecords(args.records,'user_import'),task=createTask(args.topic,d);return {...task,report_markdown:reportMarkdown(task)};}throw new AppError('UNKNOWN_TOOL','未知工具名称');}
