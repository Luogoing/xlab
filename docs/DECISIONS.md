# 0.2 决策

- 使用用户指定网关获取 Patentics 数据；Codex 负责分析，GLM 显式选择且不得自动回退。
- 0.1 共享离线核心保留；新增 server 模块，新增行为通过本机 HTTP 服务。
- 证据校验与模型推断分开：引文一致可验证，推断语义留待人工复核。
- 所有报告格式使用冻结报告；版本 1 备份继续读取，模型恢复按回放标记。
- 默认 32 MiB 用户正文上限、网关单响应 5 MiB、串行 3.2 秒节流，循环任务不自动重试。
- 本次验收以 docs/ACCEPTANCE.md 和实际回执为准，历史文档仅供追溯。
