# 七项静态审查问题的关闭复核

复核日期：2026年9月12日，基线记录时间2026-09-12T15:20:04Z。复核对象为`code_review.md`中的CR-01至CR-07，涉及共享核心、页面恢复、筛选及导出。本次未修改Site项目代码，未操作浏览器；采用源码复查、既有自动测试和冷启动记录核对，并独立执行一次主题恢复的纯核心验证。

## 一、复核结论

七项原审查问题已完成六项实现修复和一项明确范围调整，当前未保留原范围内的阻断缺陷。主题策略、任务恢复、权利要求可见性、报告内容一致性和重复证据处理均已修改；外部扩展检索保留仅传递词项的行为，并在入口明确说明日期另设。复核期间另发现的新主题尚未运行便恢复的分支也已修复，相关自动测试已加入。本结论针对所列代码缺陷，不能扩大为所有浏览器、网络环境或真实教学场景均已通过验收。

## 二、逐项处理状态

| 编号 | 关闭状态 | 当前实现与证据 | 复核边界 |
| --- | --- | --- | --- |
| CR-01 新主题沿用旧策略 | 已修复 | 页面`run()`检查策略绑定主题；核心`normalizeStrategy()`同样检查绑定不一致时重建基础策略。测试日志新增未运行主题编辑后恢复用例；本次独立验证已有液氢任务改为电池后恢复并执行，返回3条电池记录 | 本次未重新执行浏览器输入操作；核心与页面连接逻辑已阅读 |
| CR-02 备份丢失任务成果 | 已修复 | 手工备份和本机恢复均调用`restoreWorkspace()`。恢复任务标识、版本、查询和报告历史、排除决定、筛选理由、编辑及学习模式；页面直接接受已校验任务，无需通过重新运行恢复结果。A12日志与测试源码覆盖这些字段 | 当前分析根据原始记录重新计算，历史报告标记`restored_replay`；恢复不等于重新核验外部专利原文 |
| CR-03 损坏状态在渲染中抛错 | 已修复原缺陷 | `restoreWorkspace()`在提交页面状态前规范化数据和策略，并重建当前分析；不再直接信任序列化的生成结论。页面恢复调用位于`try/catch`内。A12测试覆盖缺少必要字段、不完整任务与被篡改的旧分析 | 测试证明已覆盖异常类别的处理；未宣称任意损坏文件都经过穷尽测试 |
| CR-04 导入权利要求不可查看 | 已修复 | `claims`追加为独立来源片段，进入筛选、证据和报告；证据弹窗依据是否提供权利要求显示不同材料边界。CR04测试检查权利要求片段及报告出现实际文本 | 权利要求由用户提供，完整性与来源仍需核验，代码没有将其提升为官方已核验全文 |
| CR-05 外部链接漏传日期 | 以明确边界关闭 | 外部按钮现为扩展检索（仅词项，日期另设）；本地日期筛选和报告条件仍一致 | 外部日期自动传递未实现，属于准确披露的功能范围；本次未验证外部平台实际检索 |
| CR-06 归档与下载分别生成文本 | 已修复 | `reportMarkdown()`采用任务分析更新时间；导出先保存单一报告版本，再取该版本的固定Markdown生成MD或Word，JSON也包含同一版本。A13测试确认版本文本与报告文本完全一致 | 传输字节验证见下文；浏览器文件落盘仍未验证 |
| CR-07 重复证据ID与书签 | 已修复导入主路径 | 规范化按证据ID去重，后续报告与Word只接收唯一片段；CR07测试验证重复片段规范化为一条 | 本次未另行做Word书签点击测试；本结论针对原重复导入路径 |

## 三、实际证据与未验证事项

既有自动测试日志在复核结束时显示25项通过、0项失败、0项跳过。开始复核时日志为24项，开发者随后加入CR-01未运行主题编辑后恢复的回归用例，日志更新为25项。本次阅读了`artifacts/test-log.tap`及相关`tests/core.test.mjs`断言，确认日志对应导入、证据、恢复、导出和MCP测试内容；本次审查者没有重新执行整套测试，因此应表述为复核已存在的实际执行记录。

本次独立执行的纯核心检查验证了恢复场景下的主题一致性。具体输入为已生成的液氢任务、尚未执行的新电池主题及旧液氢策略，调用`restoreWorkspace()`后模拟页面的运行前策略检查，再调用`createTask()`。实际得到电池对象词组和3条电池记录，公开编号为US20150044523A1、WO2016131141A1、US20110212355A1，原液氢记录未混入。该检查未写入项目文件，也未涉及浏览器或外部数据调用。

HTTP传输层已有精确字节证据，浏览器最终下载仍需单列为未验证。`core/download.mjs`与`app/api/export/route.ts`提供同源导出响应；自动测试检查附件响应头和Word字节一致，`artifacts/cold-start.json`记录冷启动环境中HTTP 200、21,536字节的DOCX响应及精确字节校验通过。页面提交表单至`/api/export`，成功提示准确写为报告版本已保存、已提交文件下载请求。根据执行环境记录，浏览器下载阶段受到平台Fetch domain未启用影响，当前不能据此宣称用户浏览器已经收到并保存文件。

Word版式验收应继续引用实际渲染记录。CR-06解决固定文本及传输一致性，CR-07解决重复导入片段产生的书签问题，这两项均不证明所有导出页面已逐页检查。本次只复查导出源码，完整Word渲染结果和浏览器文件落盘结果应分别列示，避免合并成导出全部通过。

## 四、复核基线

| 文件 | SHA-256 |
| --- | --- |
| core/engine.mjs | 4540d40acc473f8fc8bf520f65e27e33ef6c3168cee8cce9a6f220398ad7671f |
| core/docx.mjs | 15e965c70d2111fc1e5898057af93e0de45c5bf93d55027a5ce74b189a07d656 |
| app/page.tsx | 289edd169b5e244a6d26460eef6958acd39bda8e7b12b97dbbc5dbdcf1d3ea5e |
| core/download.mjs | d5a7999ee2909b8a948c7215b57fdd66b982c0f60ebd607b98f4e3595b253d6f |
| artifacts/test-log.tap | 22a0de325e0933c4b2c81e819a9a515b6a0b309ef977f7f72f70466b2b75b76f |

本轮复核到此收口，剩余工作应围绕已有明确验证缺口执行。原七项问题无需继续作为未修复阻断项保留；浏览器实际文件下载、Word逐页渲染和真实课堂试点分别按其自身证据记录完成或未完成，避免以增加无关建议延迟已具备条件的交付。
