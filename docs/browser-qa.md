> 历史 0.1 资料。0.2 当前状态与验收见 README.md、docs/ACCEPTANCE.md。

# 浏览器验收交接记录

本记录将2026年9月12日已执行操作与本地待执行步骤分开保存，2026年9月14日未重新运行浏览器。以下历史操作依据本会话执行记录整理，保留的截图仅位于artifacts/screenshots/desktop-results.jpg；当前没有将完整历史浏览器事件日志导出到工程，不应把本记录描述为可自动重放脚本。

历史检查覆盖了主要研究交互。页面显示8条公开快照和规则模式；运行液氢得到5条结果、6处证据，查看首条记录可见原文及定位。直接改电池主题得到3条，编辑报告标题和结论可在预览看到；取消保留US20150044523A1后剩2条，设置frame排除词后剩1条，查询历史显示相应版本。新标签页刷新恢复任务；无关桥梁主题显示0条，实际键盘清空主题后提示请输入具体研究主题。

云端浏览器的文件保存捕获没有通过，交接必须保留这一缺口。早期Blob下载等待超时，改用同源表单后出现Protocol error (Fetch.failRequest): Fetch domain is not enabled，未取得下载落盘路径。导出归档和HTTP传输另有通过证据，但不能替代用户文件系统中的实际文件。此前文件选择器导入操作在环境断连前结果未确认，不能补记为通过。

本地补测应按下表逐步执行并记录软件版本、实际结果与证据路径。表中预期是待执行验收条件，不能提前填入通过。

| 编号 | 操作 | 预期与需要保存的证据 |
| --- | --- | --- |
| B01 | 启动服务并打开本机页面 | 记录OS、Node、浏览器版本、URL及截图；显示快照与规则 |
| B02 | 运行液氢，再直接改电池 | 5/6转为3/3，主题、策略、报告一致 |
| B03 | 改关键词、公开日期与排除词 | 数量随条件变化，报告记录实际条件 |
| B04 | 导入docs/handoff/import-example.json及自建合法CSV | 显示user_import；解析失败时原有效状态可继续使用 |
| B05 | 打开证据并点击来源 | 编号、文本、定位对应，来源失败如实记录 |
| B06 | 取消保留一条，写筛选理由，编辑标题和结论 | 当前数量、方案、报告更新；保留截图 |
| B07 | 下载Word、MD、证据JSON | 实际文件存在、非空、版本/内容/模式一致；Word打开并逐页检查 |
| B08 | 任务备份下载后，在新工作区载入 | 保留全dataset、任务ID、人工排除、理由、用户编辑及报告历史；历史标回放 |
| B09 | 恢复后只编辑新主题尚未运行，再备份恢复并执行 | 不沿用旧主题词表，结果与新主题一致 |
| B10 | 无关主题与空主题 | 0结果无虚构结论；空主题可操作错误提示 |
| B11 | 学习题、反馈输入、匿名反馈导出 | 题目反馈正确，文件真实保存；注意刷新前未导出的输入不会自动恢复 |
| B12 | 设置390×844或记录实际窄视口 | 主输入、运行、证据弹窗、下载可操作，无遮挡/溢出；保存截图 |
| B13 | 超大导出/坏备份、关闭服务再点导出 | 如实记录HTTP与页面行为；改进前端错误回执必要时新增缺陷 |
| B14 | OS级断网，保留本机服务 | 样本、导入、报告仍可操作；外部链接失败不得伪装成功 |

本地完成后应新增带日期的结果记录，并在ACCEPTANCE中只关闭有直接证据的子项。上传解析、下载传输、保存文件、办公软件打开和逐页检查应分别记录，以便区分故障所属层级。
