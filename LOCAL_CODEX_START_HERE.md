# 本地 Codex 接手执行指令

请接手当前目录中的专利研习项目，完整读取HANDOFF_LOCAL_CODEX.md、docs/taskbook/专利情报AI产品_完整开发执行提示词.md、PROJECT_STATE.md、docs/ACCEPTANCE.md和README.md，再检查当前目录及上级生效的AGENTS.md。以原任务书确定目标，以当前文件和实际运行结果确定完成状态；不要重新搭建同类项目，不要将云端旧路径作为本地运行依赖。

先执行node scripts/verify-handoff.mjs核对交付文件，再检查Node版本，以node scripts/serve.mjs启动预构建网页。运行node --test --test-reporter=tap tests/core.test.mjs tests/mcp.test.mjs，分别执行液氢和电池CLI示例，核对5条/6处证据与3条/3处证据的预期。详细命令、Windows端口设置、开发依赖版本差异及构建入口见完整交接文档。首次运行预构建包无需安装npm依赖或配置模型密钥。

随后实际完成本机Chrome或Edge的主流程验收，优先补测Word、Markdown、证据JSON、任务备份和反馈的下载落盘，以及备份载入、人工筛选恢复、JSON/CSV文件选择器和窄屏。已经通过的核心测试只能说明其覆盖的条件，不能据此自动关闭浏览器或课堂验收。新增结果保存原始日志、截图和文件，更新PROJECT_STATE.md与docs/ACCEPTANCE.md，保留历史记录。

网页、CLI和MCP必须继续复用core中的同一业务能力。当前实时数据适配器和真实模型提供方未实现；core/adapters.mjs只提供边界，填写密钥不会自动接通。先核验本地Codex的MCP配置方式并接入adapters/mcp-stdio.mjs，用真实公开样本调用工具，明确结果保存和网页续接方式。后续在具备授权与配置时再接入一个真实数据源和模型，记录调用来源、数据范围、失败状态和费用；缺少接口时继续完成其他工程工作。

保持真实记录、公开快照、用户导入和测试数据的明确区别，并分别标注实时模型、规则处理和回放。不得编造专利、原文引用、用户访谈、课堂测试、运行日志或发布状态。现有8条样本包含9处短证据，当前不具备全球全库检索、权属判定、侵权判断或创新空白证明能力。

请自主处理可逆的实现和文档决策，避免逐项询问技术选型。只有不可替代授权、实际支出、对外发布或根本方向冲突需要集中提出具体问题；其他具备条件的工作持续执行。正式文稿采用连续完整论述，每段首句概括全段，避免机械对照句、无必要引号和碎片化排版；Word修改后逐页渲染检查。每阶段交付可检查文件与真实测试结果，明确已实现、已验证和待完成的区别。
